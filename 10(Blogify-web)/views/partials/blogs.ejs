<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=, initial-scale=1.0">
    <title>Blog</title>
</head>
<body>
    <%- include('./partials/navbar.ejs') %>

    <div class="max-w-4xl mx-auto p-8 mt-10 bg-white shadow-md rounded-lg">
        <h1 class="text-4xl font-bold mb-6"><%= blogTitle %></h1>
    
        <!-- Blog Content -->
        <div class="mb-6">
          <p class="text-gray-800 text-lg leading-relaxed whitespace-pre-line"><%= blogContent %></p>
        </div>
    
        <!-- Display Uploaded Image (if exists) -->
        <% if (blogFile) { %>
          <div class="mb-6">
            <h2 class="text-xl font-semibold mb-4">Uploaded Image/File:</h2>
            <img src="/uploads/<%= blogFile %>" alt="Uploaded Blog Image" class="max-w-full rounded-lg shadow-md"/>
          </div>
        <% } %>
    
        <!-- Back Button -->
        <a href="/" class="inline-block mt-6 bg-blue-500 text-white py-2 px-4 rounded-lg hover:bg-blue-600 transition-colors duration-300">Back to Blog Form</a>
    </div>

    <%- include('./partials/script.ejs') %>
</body>
</html>